settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: myadmin_gin_gqlgen_nuxt
  path: .
  commands: {}
  watcher:
    extensions:
    - go
    paths:
    - /
    ignore:
      paths:
      - .git
      - .realize
      - vendor
